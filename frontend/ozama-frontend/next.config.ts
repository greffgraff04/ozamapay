/** @type {import('next').NextConfig} */
const nextConfig = {
  // Sa a ap pèmèt telefòn ou konekte sou pwojè a
  allowedDevOrigins: ['192.168.1.116'],
  // Si ou gen lòt konfigirasyon tankou imaj, ou ka mete yo anba a
}

module.exports = nextConfig